<%= form_with(model: roomcheckout, local: true) do |form| %>
  <% if roomcheckout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(roomcheckout.errors.count, "error") %> prohibited this roomcheckout from being saved:</h2>

      <ul>
      <% roomcheckout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dayRco %>
    <%= form.date_select :dayRco %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="field">
    <%= form.label :guest_id %>
    <%= form.text_field :guest_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
